100 while dup 0 > do
    dup 15 % 0 = if
        "FizzBuzz" print
    else
        dup 3 % 0 = if
            "Fizz" print
        else
            dup 5 % 0 = if
                "Buzz" print
            else
                dup print
            endif
        endif
    endif

    1 -
end
