100 while dup 0 > do
    dup 15 swap % 0 = if
        "FizzBuzz" print
    endif

    dup 3 swap % 0 = if
        "Fizz" print
    endif

    dup 5 swap % 0 = if
        "Buzz" print
    endif

    dup print
    1 -
end
