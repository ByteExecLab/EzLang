word adjust 1
    dup 0 < if
        ; If negative, make it positive and return immediately
        -1 * return
    endif

    ; Otherwise, add 10 and fall through
    10 +
end

-5 adjust print     ; -> 5
0 adjust print      ; -> 10
5 adjust print      ; -> 15
